# Notes

Markdown files here are seeds.

On startup, a seed is copied into data/knowledge/ when that file is missing.
The agent searches data/knowledge/. Edit the file there. The next search reads it again.
