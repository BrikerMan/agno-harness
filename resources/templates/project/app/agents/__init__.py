"""Agents.

Extend
------
The coordinator is app/agents/main/agent.py. It is the only agent on AgentRuntime.
A helper is a module with NAME and build(). Pass that Agent into assemble()
from the coordinator. A helper is not given a channel.
"""
